---
name: idea-agent
description: Analyzes the Apify Store to find underserved monetizable gaps, checks for duplicate actors, and drafts 5 unique B2B Actor ideas. Use when selecting or planning a new actor to build under the 'Pilot Family' naming suite.
---

# Idea Agent

Turns rough Actor ideas into viable Apify Actor concepts.

Outputs:

- `reports/{actor-name}/actor-concept.md`

Implemented in `shared/src/agents.ts` as `makeConcept`.
